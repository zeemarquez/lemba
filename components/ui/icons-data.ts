// This is a minimal icons-data file
// In production, this should contain the full list from lucide-icons
// For now, we'll dynamically load icons from lucide-react
export const iconsData: Array<{
  name: string;
  categories: string[];
  tags: string[];
}> = [];

// We'll populate this dynamically from lucide-react
// This is a placeholder - the actual implementation will load icons at runtime
